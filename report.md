# Codebase Change Report

## Sidebar

- Refactored `SideBar.jsx` to use semantic HTML and `react-router-dom`'s `Link`.
- Removed table/ul/tr misuse.
- Updated `SideBar.css` for clarity, accessibility, and LCH color compliance.
- Added a visual layout comment.

## Background

- Moved all inline styles from `Background.jsx` to a new `Background.css`.
- Ensured only custom CSS and LCH color space are used.
- Added a visual layout comment.

## WelcomeBanner

- Updated `WelcomeBanner.css` for LCH color compliance and semantic clarity.
- Added a visual layout comment.

## App

- Updated `App.css` for LCH color compliance.
- Added a visual layout comment.

## General

- Ensured all color declarations use LCH color space.
- Improved accessibility and semantic structure.
- Removed all inline styles in favor of custom CSS.
